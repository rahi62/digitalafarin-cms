"use client";
import Link from "next/link";import {useEffect,useState} from "react";import PageHeader from "@/components/PageHeader";import {apiFetch,Paginated} from "@/lib/api";
export default function ContentPage(){const [rows,setRows]=useState<any[]>([]),[q,setQ]=useState("");async function load(search=""){const d=await apiFetch<Paginated<any>>(`/content/entries/?ordering=-updated_at${search?`&search=${encodeURIComponent(search)}`:""}`);setRows(d.results)}useEffect(()=>{load()},[]);return <><PageHeader title="محتوا" description="مدیریت Page، Post و Content Typeهای سفارشی" action={<Link href="/content/new" className="btn">محتوای جدید</Link>}/><div className="panel"><div style={{display:"flex",gap:10,marginBottom:16}}><input placeholder="جستجو..." value={q} onChange={e=>setQ(e.target.value)} style={{flex:1,padding:10,border:"1px solid #d0d5dd",borderRadius:10}}/><button className="btn secondary" onClick={()=>load(q)}>جستجو</button></div><div className="tableWrap"><table className="table"><thead><tr><th>عنوان</th><th>مسیر</th><th>نوع</th><th>وضعیت</th><th></th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.title}</td><td dir="ltr">{x.path}</td><td>{x.content_type_slug}</td><td><span className={x.status==="published"?"badge green":"badge"}>{x.status}</span></td><td><Link className="btn secondary" href={`/content/${x.id}`}>ویرایش</Link></td></tr>)}</tbody></table></div></div></>}
