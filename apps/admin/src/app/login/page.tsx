"use client";
import {FormEvent,useState} from "react"; import {login} from "@/lib/api";
export default function LoginPage(){const [username,setUsername]=useState("admin"),[password,setPassword]=useState("admin12345"),[error,setError]=useState(""); async function submit(e:FormEvent){e.preventDefault();setError("");try{await login(username,password);location.href="/"}catch(err:any){setError(err.message)}}return <div className="loginPage"><form className="loginCard" onSubmit={submit}><h1>ورود به SEO CMS</h1><p>پنل مدیریت محتوای DigitalAfarin</p>{error&&<div className="error">{error}</div>}<div className="field"><label>نام کاربری</label><input value={username} onChange={e=>setUsername(e.target.value)}/></div><div className="field"><label>رمز عبور</label><input type="password" value={password} onChange={e=>setPassword(e.target.value)}/></div><button className="btn" style={{width:"100%"}}>ورود</button></form></div>}
