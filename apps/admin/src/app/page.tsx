"use client";
import {useEffect,useState} from "react"; import PageHeader from "@/components/PageHeader"; import StatCard from "@/components/StatCard"; import {apiFetch,Paginated} from "@/lib/api";
export default function Dashboard(){const [stats,setStats]=useState({sites:0,entries:0,keywords:0,redirects:0});const [latest,setLatest]=useState<any[]>([]);useEffect(()=>{Promise.all([apiFetch<Paginated<any>>("/sites/"),apiFetch<Paginated<any>>("/content/entries/?ordering=-updated_at"),apiFetch<Paginated<any>>("/seo/keywords/"),apiFetch<Paginated<any>>("/seo/redirects/")]).then(([s,e,k,r])=>{setStats({sites:s.count,entries:e.count,keywords:k.count,redirects:r.count});setLatest(e.results.slice(0,7))}).catch(()=>{})},[]);return <><PageHeader title="داشبورد" description="نمای کلی محتوا و وضعیت SEO سایت‌ها"/><div className="grid4"><StatCard label="سایت‌ها" value={stats.sites}/><StatCard label="محتوا" value={stats.entries}/><StatCard label="کلمات کلیدی" value={stats.keywords}/><StatCard label="ریدایرکت‌ها" value={stats.redirects}/></div><div className="panel"><h2>آخرین محتواهای ویرایش‌شده</h2><div className="tableWrap"><table className="table"><thead><tr><th>عنوان</th><th>مسیر</th><th>نوع</th><th>وضعیت</th></tr></thead><tbody>{latest.map(x=><tr key={x.id}><td>{x.title}</td><td dir="ltr">{x.path}</td><td>{x.content_type_slug}</td><td><span className={x.status==="published"?"badge green":"badge"}>{x.status}</span></td></tr>)}</tbody></table></div></div></>}
