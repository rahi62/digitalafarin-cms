"use client";
import {useEffect,useState} from "react";import PageHeader from "@/components/PageHeader";import {apiFetch,Paginated} from "@/lib/api";
export default function SeoPage(){const [rows,setRows]=useState<any[]>([]);async function load(){const d=await apiFetch<Paginated<any>>("/seo/meta/");setRows(d.results)}useEffect(()=>{load()},[]);async function analyze(id:string){await apiFetch(`/seo/meta/${id}/analyze/`,{method:"POST",body:"{}"});load()}return <><PageHeader title="سئو صفحات" description="Meta، Canonical، Robots و تحلیل پایه هر URL"/><div className="panel"><div className="tableWrap"><table className="table"><thead><tr><th>صفحه</th><th>Title</th><th>Focus keyword</th><th>Score</th><th></th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.entry_title}</td><td>{x.title||"—"}</td><td>{x.focus_keyword||"—"}</td><td><span className={x.seo_score>=70?"badge green":"badge warn"}>{x.seo_score}</span></td><td><button className="btn secondary" onClick={()=>analyze(x.id)}>Analyze</button></td></tr>)}</tbody></table></div></div></>}
