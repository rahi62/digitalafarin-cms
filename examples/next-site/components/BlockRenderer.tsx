import type {CmsBlock} from "@digitalafarin/cms-next";
export function BlockRenderer({blocks}:{blocks:CmsBlock[]}){return <>{blocks.map((b,i)=>{const d=b.data as any;switch(b.type){case "hero":return <section key={b.id||i} style={{padding:"60px 0"}}><h1>{d.title}</h1><p>{d.subtitle}</p></section>;case "heading":{const level=Math.min(6,Math.max(2,Number(d.level)||2));const Tag=`h${level}` as keyof JSX.IntrinsicElements;return <Tag key={b.id||i}>{d.text}</Tag>}case "paragraph":return <p key={b.id||i}>{d.text}</p>;case "image":return <img key={b.id||i} src={d.url} alt={d.alt||""} style={{maxWidth:"100%"}}/>;case "faq":return <section key={b.id||i}>{(d.items||[]).map((x:any,j:number)=><details key={j}><summary>{x.question}</summary><p>{x.answer}</p></details>)}</section>;default:return null}})}</>}
